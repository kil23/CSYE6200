package edu.neu.csye6200;

import java.util.ArrayList;
import java.util.List;

public class ZooAnimalAPI {
	private List<AnimalAPI> list = null;
	{
		list = new ArrayList<>();
	}
	
	public void add(AnimalAPI animal) {
		this.list.add(animal);
	}
	
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		
		sb.append(this.list.size() + " animals in zoo.").append("\n");
		for (AnimalAPI animal: list) {
			sb.append(" ").append(animal.toString()).append("\n");
		}
		
		return sb.toString();
	}
	
	public static void demo() {
		AnimalAPI animal = new AnimalAPI();
		Cat cat = new Cat();
		Dog dog = new Dog();
		Fish fish = new Fish();
		
//		List<AnimalAPI> list = new ArrayList<>();
//		list.add(animal);
//		list.add(cat);
//		list.add(dog);
//		list.add(fish);
		
		ZooAnimalAPI zoo = new ZooAnimalAPI();
		zoo.add(animal);
		zoo.add(cat);
		zoo.add(dog);
		zoo.add(fish);
		System.out.println(zoo);	
		
		for(AnimalAPI animalAPI : list) {
			animalAPI.speak();
		}
	}
}
